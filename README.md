This repository contains a simple C program that demonstrates a potential infinite loop bug. The program uses a while loop to print the numbers from 0 to 9, but it does not properly increment the loop counter variable. This can cause the loop to run indefinitely.